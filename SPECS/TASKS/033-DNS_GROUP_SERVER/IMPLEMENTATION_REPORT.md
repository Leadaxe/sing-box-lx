# IMPLEMENTATION_REPORT 033 — TTL-модель, режимы (v2)

## Файлы

| Зона | Файлы |
|------|-------|
| lx-пакет | `dns/transport/group/group.go` (записи TTL, выбор цели, единый поток, бюджет, survival, state-снимок), `fan.go` (веер: спасение/выборы/parallel, guard, single-flight), тесты `group_test.go`/`fan_test.go` |
| `// lx:` upstream | `option/dns.go` — состав полей `GroupDNSServerOptions` внутри существующего блока (`ErrorTTL`/`WinTTL` вместо `Interval`/`DownTime`); `constant/dns.go`, `include/registry.go` — без изменений |
| Live-тесты | `test/dns_group_lx_test.go` (+ негативные на конфиг v1), `test/dns_group_trace_lx_test.go` |

`race.go`/`race_test.go` удалены — режим `race` растворился в `fastest`.

## Адверсариальная ревизия диффа (24 агента: 4 линзы → верификация)

16 подтверждённых находок (6 уникальных) — все исправлены до закрытия:

1. **(nil, nil) из веера** (critical): все сбои веера заброшены guard'ом →
   `E.Errors()` пустого среза = nil → nil-deref в клиенте. Fix: не-nil
   ошибка в вырожденной ветке + пояс в `fan()`.
2. **Дырявая Reset-амнистия** (major): `noteError`/`noteSuccess` писали без
   gen-guard — устаревший веер травил свежие таблицы. Fix: gen во всех
   записях здоровья.
3. Отрицательный `win_ttl` проходил валидацию (веер на каждый запрос).
4. Конкуренты окна выборов перезаписывали `current` (треш липкости) —
   `provisional`-выбор без переизбрания.
5. `lastRttMs` < 1мс схлопывался в «не мерялся» — кламп 0→1 (прецедент GetPool).
6. Успех веера после смерти ctx чеканил победу/current — теперь только лечит.

Регрессионные тесты на каждый фикс — в `fan_test.go`.

## DoD

- Юниты (`-race`, ×2): липкость stable / отсутствие возврата на
  выздоровевшего / спасение веером; выборы fastest / single-flight
  (бёрст = один веер) / истечение побед / стирание побед ошибкой;
  parallel без побед + каркас выживания; бюджет (blackhole-цель оставляет
  половину вееру) и guard (мёртвый бюджет не травит участников);
  выживание с ротацией по давности и восстановлением по успеху;
  NXDOMAIN-ответ/победа; Reset-амнистия (в т.ч. посреди веера).
- Живой `box.New`: конфиг v2 всех режимов; `mode: failover` и `down_time`
  отвергаются; `sing-box check` ×3 режима.
- Сборки ± теги, vet, gofmt — чисто.

## Вне скоупа

Hedged-веер; per-query-ротация; приоритет порядком; фильтрация «быстрых
лжецов»; полевой device-verify.
