# FEATURE 015 — CHAIN — виртуальная цепочка хопов из групп и узлов

| Поле | Значение |
|------|----------|
| Тип | Продуктовая фича |
| Build-tag | `with_lx_chain` (состояние цепочки в UI — за `with_lx_command`, см. [OBSERVABILITY](../006-OBSERVABILITY/FEATURE.md)) |
| Состояние | ✅ Реализовано (I): юниты + приёмочный стенд на живых shadowsocks-хопах; полевой проверки WG-звеньев не было — [073](../../TASKS/073-CHAIN_OUTBOUND/SPEC.md) |

## Назначение

Многохоповый маршрут `клиент → хоп 1 → хоп 2 → … → цель` сегодня собирается
только вручную: узлу-выходу прописывается `detour` на узел-вход, и связь
зашивается в сами узлы. Узел, нужный в двух ролях (сам по себе и как звено),
приходится дублировать в конфиге; группа (`selector`, `urltest`) звеном быть
не может вовсе — `detour` на группу переадресует один узел, но не даёт
выбирать, *через что* этот узел идёт.

Фича добавляет outbound типа `chain`: упорядоченный список позиций, каждая
из которых — узел **или группа**. Цепочка собирается в рантайме из того, что
группы выбрали прямо сейчас; конфиг узлов не дублируется; переключение
группы на любой позиции меняет путь без перезапуска. Снаружи цепочка —
обычный outbound: её можно положить в маршрут, в `selector`, в `urltest`.

## Контролируемые параметры

```json
{
  "type": "chain",
  "tag": "virtualisation",
  "outbounds": ["selector-in", "selector-mid", "selector-exit"],
  "idle_timeout": "5m",
  "strip_evasion": true,
  "strip": { "multiplex.padding": false, "tls.utls": true },
  "rewrite": { "wireguard": { "mtu": 1200 } }
}
```

| Ключ | Тип | Дефолт | Смысл |
|------|-----|--------|-------|
| `outbounds` | список тегов, ≥ 2 | обязательное | Позиции цепочки **в порядке прохождения пакета**: `[0]` — первый хоп от клиента (касается реальной сети), последний — узел, чей адрес видит цель. Любая позиция — узел, endpoint или группа любой вложенности |
| `idle_timeout` | длительность | `5m` | Через сколько простоя удаляется рантайм-экземпляр звена; `0` — не удалять до остановки |
| `strip_evasion` | bool | `true` | Снимать у звеньев на позициях ≥ 1 односторонние DPI-приёмы (каталог ниже) |
| `strip` | карта `ключ → bool` | `{}` | Патч к каталогу поверх `strip_evasion`: `false` — не снимать, `true` — снимать дополнительно. Неизвестный ключ — ошибка старта |
| `rewrite` | карта `тип узла → JSON-патч` | `{}` | Переопределение конфига звеньев **по типу** (merge-patch поверх конфига узла); действует на позициях ≥ 1 |

Порядок позиций — единственное, что нужно запомнить: **не** «кто через
кого», как у `detour`, а «в каком порядке идёт пакет».

### Каталог `strip`

| Ключ | По умолчанию | Что снимается | Почему это односторонне |
|------|--------------|---------------|-------------------------|
| `tls.fragment` | снимается | пакетная фрагментация ClientHello (`fragment`, `fragment_fallback_delay`) | сервер её не видит; внутри туннеля даёт задержки и точки отказа. **`record_fragment` не трогается** — под `detour` он включается автоматически как защита пути (см. [HOTFIXES / 060](../004-HOTFIXES/FEATURE.md)) и снятию не подлежит |
| `multiplex.padding` | снимается | паддинг мультиплексора | запрашивается клиентом; сервер одинаково принимает без него |
| `xhttp.padding` | снимается | паддинг XHTTP — к минимуму диапазона, обфус-режим выкл. | сервер допускает любую длину |
| `tls.utls` | **не** снимается | имитация отпечатка ClientHello | не источник отказов; снимать можно через `strip`, кроме узлов с `reality` (обязателен — ошибка старта) |

В каталог **не входят** параметры-контракты с сервером: `flow`, `obfs`,
`shadowtls`, `plugin`, `udp_over_tcp`, `ech`, пути/хосты транспортов. Их
цепочка не трогает никогда.

### Сборка

Тег `with_lx_chain`; без тега `type: chain` отвергается на чтении конфига,
остальное ядро ведёт себя как upstream. Выдача состояния цепочки клиенту —
за `with_lx_command` по режиму расширений протокола.

## Входы

- Конфиг цепочки и конфиги узлов/групп по тегам позиций (цепочка читает
  их, не меняя оригиналов).
- Текущий выбор каждой группы на позициях — то, что группа выбрала бы для
  обычного соединения, со всей её логикой (ручной выбор, health-check,
  sticky, штрафы, fallback).
- Соединения из маршрутизатора (TCP/UDP) — как у любого outbound.

## Выходы

- Провод: одно вложенное соединение — транспорт позиции 0 несёт транспорт
  позиции 1, тот — позиции 2, и так до цели. Каждый следующий хоп — полезная
  нагрузка предыдущего.
- Наблюдаемость: разрешённый путь соединения (какой узел на какой позиции)
  в хвосте `detourList`; состояние цепочки — по позициям: выбранный узел,
  состояние звена (нет / поднимается / активно / уснуло / простаивает),
  живые соединения, эффективный MTU и что снято/переписано; счётчики
  дозвонов, ошибок по позициям, созданий и удалений звеньев.
- Ошибки дозвона — с указанием позиции и пары «через что»:
  `chain[tag] #2 (warp-exit) via #1 (node-m): …`.

## Data flow

```
конфиг ──► СТАРТ
   │  валидация: ≥2 позиции, теги есть, типы звеньев на позициях ≥1
   │  допустимы (узел с dial-полями | direct | block | группа из таких);
   │  strip/rewrite применимы к КАЖДОМУ достижимому узлу (сухой прогон);
   │  цикл «группа содержит цепочку» — отказ
   │  прогрев: звенья детерминированных позиций (узлы, селекторы — не urltest)
   ▼
соединение ──► ПОЗИЦИЯ n−1 (выход)
   │  узел ──────► его ЗВЕНО (рантайм-экземпляр «узел через позицию n−2»)
   │  группа ────► группа выбирает по своей логике ──► ЗВЕНО выбранного узла
   │  direct ────► прозрачно: сразу в позицию n−2 (хоп «выключен»)
   │  block ─────► соединение отвергнуто
   ▼  ... звено дозванивается до СВОЕГО сервера через позицию i−1 ...
ПОЗИЦИЯ 0 (вход) ──► оригинал как есть: его dial-поля, его сеть, без звена
   ▼
цель видит адрес выходного узла

ЗВЕНО = (позиция, узел): создаётся при первом выборе (или прогревом),
   конфиг = оригинал − strip + rewrite, затем MTU понижен на накладные
   IP-туннелей под ним; тот же тег, что у оригинала (история/штрафы/sticky
   группы работают без правок); живёт, пока есть соединения ИЛИ его выбирали
   недавнее idle_timeout; уснувший WG-туннель звена спит по правилам ENERGY
ГРУППЫ НЕ КОПИРУЮТСЯ никогда; звенья — только у узлов на позициях ≥ 1
```

## Правила и гарантии

- **Порядок = порядок пакета.** `outbounds[0]` — вход, последний — выход.
- **Позиция 0 не изменяется.** Её dial-поля (`bind_interface`, `routing_mark`,
  …) действуют; ни `strip`, ни `rewrite`, ни MTU её не касаются.
- **Группы не копируются.** Вся логика выбора — оригинальной группы: ручной
  выбор, health-check, sticky, штрафы и fallback, `interrupt_exist_connections`.
  Переключение группы с этим флагом рвёт и соединения цепочки, прошедшие
  через неё, включая туннели звеньев выше — они переподнимаются через новый
  выбор.
- **Звено = оригинал минус DPI-обвес плюс rewrite, с подогнанным MTU.**
  Порядок преобразований фиксирован: `strip` → `rewrite` → MTU. `mtu` в
  конфиге узла трактуется как «MTU как самостоятельного»; цепочка только
  **понижает** его на точные накладные IP-туннелей под звеном (WG внутри
  IP-туннеля −60/−80 по семейству адреса сервера, MASQUE ≈ −90); над
  потоковыми и датаграммными прокси MTU не меняется. Под группой берётся
  худший случай по всем её участникам — звено не пересоздаётся при
  переключении группы ниже.
- **Прозрачный `direct`.** На позиции ≥ 1 `direct` — «хопа нет»: путь
  укорачивается на эту позицию. `direct` участником селектора = выключатель
  позиции на лету. `block` — терминален на любой позиции.
- **Ленивое создание, детерминированный прогрев.** Звенья создаются при
  первом выборе узла; на старте поднимаются только звенья позиций, чей
  выбор известен (узлы, селекторы любой вложенности без urltest на пути).
  Ошибка прогрева = ошибка старта.
- **Удаление по простою, не по переключению.** Звено удаляется только при
  нуле живых соединений **и** отсутствии выбора дольше `idle_timeout`;
  живой поток через старый выбор держит звено. Уснувший туннель звена —
  состояние ENERGY, не удаление.
- **Fail-fast на старте:** неизвестный ключ `strip`; `rewrite`, который не
  декодируется в конфиг хотя бы одного достижимого узла; `tls.utls: true`
  при узле с `reality`; недопустимый тип на позиции ≥ 1; коллизия тега
  цепочки с пользовательскими тегами; цикл через группы.
- **Внутренние теги.** Цепочка резервирует теги вида `<tag>#<i>`; они
  адресуемы для проверки задержки по префиксу пути, но не входят в списки
  outbound'ов.

## Границы

- **Группа на позиции ≥ 1 ранжирует по прямым замерам** своих узлов, не
  «через нижние хопы». Задержку пути по слоям даёт проверка внутренних
  тегов `<tag>#0`, `#1`, … Пассивный фидбек и штрафы от дозвонов цепочки
  приписываются тегу узла — общему для прямой и цепочечной роли.
- **UDP-способность нижнего хопа не проверяется статически**: туннель на
  позиции i над узлом без UDP на позиции i−1 даёт ошибку дозвона с указанием
  позиций, не ошибку старта.
- **Sticky urltest на позиции ≥ 1** ключуется адресом сервера узла над ней,
  не конечной целью.
- **Вложенная цепочка** допустима только на позиции 0.
- **DPI между хопами** (`[domestic-relay, foreign-node]`) требует
  `"strip": {"tls.fragment": false}` — дефолт рассчитан на DPI между
  клиентом и входом.
- **Туннель над датаграммным прокси** (hy2, tuic, ss-UDP) сохраняет `mtu`
  конфига — физический путь неизвестен, как и при обычном `detour`; над
  `tuic` в `native`-режиме выдаётся предупреждение.
- **Первый дозвон к новому выбору** платит создание звена синхронно (для WG —
  старт стека и хендшейк).
- Серверная сторона, per-позиционные переопределения вне `rewrite`-по-типу,
  лимиты числа звеньев — не делаем.

## Задачи фичи

| Задача | Что даёт |
|--------|----------|
| [073](../../TASKS/073-CHAIN_OUTBOUND/SPEC.md) | Outbound `chain`: позиции-группы, рантайм-звенья, прозрачный `direct`, прогрев/простой, MTU, `strip`/`rewrite`, наблюдаемость |
